<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="homepage.css">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link
        href="https://fonts.googleapis.com/css2?family=Poppins:ital,wght@0,100;0,200;0,300;0,400;0,500;0,600;0,700;0,800;0,900;1,100;1,200;1,300;1,400;1,500;1,600;1,700;1,800;1,900&display=swap"
        rel="stylesheet">
</head>

<body>
    <div class="container" id="container">
        <div class="navbar">
            <h1>E-commerce</h1>
            <div class="profile-div">
            <button class="profile" id="myProfile"><%= name[0] %></button>
            <button class="logout" id= <%= name %> onclick = logOut(this)>Logout</button>
        </div>
        </div>
        <div class="product-section" id="product-section">

            <% for (let i=0; i < array.length; i++) { %>
                <div class="product">
                    <div class="image">
                        <img src="<%= array[i].productImage %>" alt="">
                    </div>
                    <div class="product-name">
                        <%= array[i].productName %>
                    </div>
                    <div class="buttons">
                        <button class="cart">ADD TO CART</button>
                        <button class="detail" id=<%=array[i]._id %> onclick = "viewDetails(this)">View
                            Details</button>
                    </div>


                </div>
                <% } %>

        </div>
        <button id="loadmore">Load More</button>
    </div>
    <script src="homepage.js"></script>
</body>

</html>